# Disable script execution in the uploads directory
<FilesMatch "\.(php|php[0-9]|phtml|pl|py|jsp|asp|sh|cgi|js|html|htm)$">
    Require all denied
</FilesMatch>

# Disable directory indexing (redundant but safe)
Options -Indexes
